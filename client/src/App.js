import React from 'react';
import { Navigate } from 'react-router-dom';
import MainScreen from './Pages/MainScreen';



const App = ({ results }) => (
  <div>
  
      <MainScreen/>
   
  </div>
  )

export default App;